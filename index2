<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blair Forest — Where Technology Meets the Wild</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --forest-dark: #1a2f1a;
            --forest-green: #2d4a2d;
            --solar-gold: #d4af37;
            --mist-gray: #f5f5f0;
            --charcoal: #2c2c2c;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--mist-gray);
            color: var(--charcoal);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            background: linear-gradient(135deg, var(--forest-dark) 0%, var(--forest-green) 50%, #1a1a2e 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 80%, rgba(212, 175, 55, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(45, 74, 45, 0.3) 0%, transparent 50%);
            pointer-events: none;
        }

        .logo-container {
            position: relative;
            z-index: 2;
            text-align: center;
            margin-bottom: 2rem;
        }

        .logo-placeholder {
            width: 200px;
            height: 200px;
            border: 3px solid var(--solar-gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            font-family: 'Cormorant Garamond', serif;
            font-size: 4rem;
            color: var(--solar-gold);
            text-shadow: 0 0 30px rgba(212, 175, 55, 0.5);
        }

        .hero h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 400;
            color: var(--white);
            letter-spacing: 0.1em;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
        }

        .hero .tagline {
            font-family: 'Cormorant Garamond', serif;
            font-style: italic;
            font-size: clamp(1.2rem, 3vw, 1.8rem);
            color: var(--solar-gold);
            letter-spacing: 0.15em;
            opacity: 0.9;
        }

        .hero-stats {
            display: flex;
            gap: 3rem;
            margin-top: 4rem;
            flex-wrap: wrap;
            justify-content: center;
            position: relative;
            z-index: 2;
        }

        .stat {
            text-align: center;
            padding: 1.5rem 2rem;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            border: 1px solid rgba(212, 175, 55, 0.2);
            min-width: 150px;
        }

        .stat-value {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.5rem;
            color: var(--solar-gold);
            display: block;
        }

        .stat-label {
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.7);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        /* Features Section */
        .features {
            padding: 6rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1rem;
            color: var(--forest-dark);
        }

        .section-subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 4rem;
            font-weight: 300;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: var(--white);
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--forest-green), var(--solar-gold));
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            color: var(--forest-dark);
            margin-bottom: 0.75rem;
        }

        .feature-card p {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.7;
        }

        /* Solar Section */
        .solar-section {
            background: var(--forest-dark);
            color: var(--white);
            padding: 6rem 2rem;
            position: relative;
            overflow: hidden;
        }

        .solar-section::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.1) 0%, transparent 70%);
            pointer-events: none;
        }

        .solar-container {
            max-width: 1000px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .solar-content h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--solar-gold);
        }

        .solar-content p {
            opacity: 0.9;
            margin-bottom: 2rem;
            line-height: 1.8;
        }

        .solar-stats {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        .solar-stat {
            background: rgba(255, 255, 255, 0.05);
            padding: 1.5rem;
            border-radius: 12px;
            text-align: center;
            border: 1px solid rgba(212, 175, 55, 0.2);
        }

        .solar-stat-value {
            font-size: 2rem;
            font-weight: 600;
            color: var(--solar-gold);
            display: block;
        }

        .solar-stat-label {
            font-size: 0.8rem;
            opacity: 0.7;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Build Progress */
        .build-section {
            padding: 6rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .timeline {
            position: relative;
            padding-left: 3rem;
        }

        .timeline::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 2px;
            background: linear-gradient(180deg, var(--forest-green), var(--solar-gold));
        }

        .timeline-item {
            position: relative;
            margin-bottom: 3rem;
            padding-left: 2rem;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -3rem;
            top: 0.5rem;
            width: 12px;
            height: 12px;
            background: var(--solar-gold);
            border-radius: 50%;
            border: 3px solid var(--white);
            box-shadow: 0 0 0 3px var(--solar-gold);
        }

        .timeline-date {
            font-size: 0.85rem;
            color: var(--forest-green);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        .timeline-item h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            margin: 0.5rem 0;
            color: var(--forest-dark);
        }

        .timeline-item p {
            color: #666;
        }

        /* Footer */
        footer {
            background: var(--charcoal);
            color: rgba(255, 255, 255, 0.7);
            padding: 3rem 2rem;
            text-align: center;
        }

        footer p {
            font-size: 0.9rem;
        }

        footer .location {
            color: var(--solar-gold);
            margin-top: 0.5rem;
            font-family: 'Cormorant Garamond', serif;
            font-style: italic;
        }

        @media (max-width: 768px) {
            .solar-container {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
            
            .hero-stats {
                gap: 1rem;
            }
            
            .stat {
                min-width: 120px;
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="logo-container">
            <img src="whiteLogo.png" alt="Blair Forest Logo" style="width: 250px; height: auto; margin-bottom: 2rem; filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.3));">
            <h1>Blair Forest</h1>
            <p class="tagline">Where Technology Meets the Wild</p>
        </div>
        
        <div class="hero-stats">
            <div class="stat">
                <span class="stat-value">14.4</span>
                <span class="stat-label">kWh Battery</span>
            </div>
            <div class="stat">
                <span class="stat-value">7</span>
                <span class="stat-label">kW Solar Array</span>
            </div>
            <div class="stat">
                <span class="stat-value">3200</span>
                <span class="stat-label">ft² Big House</span>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <h2 class="section-title">Living Off-Grid, On Purpose</h2>
        <p class="section-subtitle">A self-sufficient sanctuary powered by the sun, built by hand, and managed by an AI.</p>
        
        <div class="feature-grid">
            <div class="feature-card">
                <div class="feature-icon">☀️</div>
                <h3>Solar Powered</h3>
                <p>EG4 6000XP inverter with 14.4kWh battery storage and a 7kW solar array. Energy independence isn't a dream here—it's the daily reality.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🏗️</div>
                <h3>The Big House</h3>
                <p>An 80×40 pole barn shop and residence rising from the Cumberland Plateau. Perma-columns set, trusses staged, and the vision becoming reality.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🍁</div>
                <h3>Maple Syrup</h3>
                <p>31 taps. 63.5 gallons of sap. 1.375 gallons of pure Tennessee maple syrup. The forest provides, and we steward.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🤖</div>
                <h3>Moltpuppet</h3>
                <p>An AI assistant managing solar monitoring, maintenance logs, and the daily Morning Brew. Technology serving the wild, not the other way around.</p>
            </div>
        </div>
    </section>

    <!-- Solar Section -->
    <section class="solar-section">
        <div class="solar-container">
            <div class="solar-content">
                <h2>Powered by the Sun</h2>
                <p>The Guest House runs on a fully independent solar system. From dawn's first light to the darkest night, we're harvesting energy from the sky and storing it in our battery bank. No grid. No bills. Just pure, clean power.</p>
                
                <div class="solar-stats">
                    <div class="solar-stat">
                        <span class="solar-stat-value">74%</span>
                        <span class="solar-stat-label">Current SOC</span>
                    </div>
                    <div class="solar-stat">
                        <span class="solar-stat-value">0W</span>
                        <span class="solar-stat-label">Pre-Dawn</span>
                    </div>
                </div>
            </div>
            <div class="solar-visual">
                <!-- Placeholder for solar dashboard or image -->
                <div style="background: rgba(255,255,255,0.05); border-radius: 16px; padding: 3rem; text-align: center; border: 1px solid rgba(212, 175, 55, 0.2);">
                    <div style="font-size: 4rem; margin-bottom: 1rem;">⚡</div>
                    <p style="opacity: 0.8; font-style: italic;">Real-time monitoring via EG4 Portal</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Build Progress -->
    <section class="build-section">
        <h2 class="section-title">The Build</h2>
        <p class="section-subtitle">From raw land to rising structure. The journey so far.</p>
        
        <div class="timeline">
            <div class="timeline-item">
                <span class="timeline-date">2026</span>
                <h3>Land Clearing & Access</h3>
                <p>Cleared the build site with the Bobcat E85. Established the road up to the Big House location. First major earthwork completed.</p>
            </div>
            
            <div class="timeline-item">
                <span class="timeline-date">Feb 2026</span>
                <h3>Perma-Columns & Trusses</h3>
                <p>Concrete perma-columns delivered and set. 32 trusses staged on-site using the excavator. Ready for the big lift.</p>
            </div>
            
            <div class="timeline-item">
                <span class="timeline-date">Coming Soon</span>
                <h3>The Big Lift</h3>
                <p>Truss installation, wall framing, and the shell of the 80×40 structure taking shape. The vision becomes three-dimensional.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>Blair Forest — A Work in Progress</p>
        <p class="location">Beersheba Springs, Tennessee</p>
        <p style="margin-top: 1rem; font-size: 0.8rem; opacity: 0.5;">Powered by Sun & Code</p>
    </footer>
</body>
</html>
